
/*
 *
 * You are given a large integer represented as an integer array digits, where each
 * digits[i] is the i^th digit of the integer. The digits are ordered from most
 * significant to least significant in the left-to-right order. The larger integer
 * doest not contain any leadings 0's.
 *
 * Increment the larger integer by one and return the resulting array of digits.
 *
 * */

#include <string>
using namespace std;

class Solution {
	public:
		vector<int> PlusOne(vector<int>& digits) {
			
			string 

		}
};
